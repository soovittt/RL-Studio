# Google Cloud Build ignore file for frontend
# Files and directories to exclude from Cloud Run deployments

# Git
.git/
.gitignore

# Node modules (will be installed during build)
node_modules/

# Build outputs (will be generated)
dist/
build/

# Environment files
.env
.env.local
.env.*.local
*.env

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/
.pytest_cache/

# Documentation (except README)
*.md
!README.md

# Backend (not needed for frontend deployment)
backend/
training/
benchmarks/

# Scripts (not needed in container)
scripts/
*.sh
!deploy-frontend-gcp.sh
# But keep the deployment script for reference (not used in build)

# Docker files (not needed, but allow Dockerfile for frontend deployment)
docker-compose*.yml
# Note: Dockerfile is needed for Cloud Run --source deployments
# !Dockerfile  # Allow Dockerfile

# Other
*.tmp
*.bak
.cache/
.venv/
venv/
backend/
